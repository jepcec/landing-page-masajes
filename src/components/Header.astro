---
const menuItems = [
  { name: "Inicio", target: "hero" },
  { name: "Nosotros", target: "about" },
  { name: "Servicios", target: "services" },
  { name: "Galería", target: "gallery" },
  { name: "Ubicación", target: "findus" },
];
---

<header class="fixed top-0 left-0 right-0 z-50 bg-teal-50/95 backdrop-blur-sm shadow-sm">
  <nav class="container mx-auto px-6 py-4 flex justify-between items-center">
    <div 
      class="text-2xl font-bold text-teal-800 cursor-pointer hover:scale-105 transition-transform"
      data-target="hero"
    >
      🌿 Tu Masaje
    </div>

    <ul class="hidden md:flex space-x-6">
      {menuItems.map((item) => (
        <li>
          <button
            data-target={item.target}
            class="text-teal-700 font-medium hover:text-green-600 px-3 py-2 rounded-full transition-colors"
          >
            {item.name}
          </button>
        </li>
      ))}
    </ul>

    <button class="md:hidden text-teal-800">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" />
      </svg>
    </button>
  </nav>
</header>

<!-- Espaciador para compensar el header fijo -->
<div class="h-16"></div>

<script>
  document.querySelectorAll('[data-target]').forEach(btn => {
    btn.addEventListener('click', () => {
      const target = document.getElementById(btn.getAttribute('data-target')!);
      target?.scrollIntoView({ behavior: 'smooth' });
    });
  });
</script>